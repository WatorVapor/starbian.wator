version: '3'
services:
  ipfs-dameon:
    image: watorvapor/js-starbian
    volumes:
      - ../ws:/home/app/ws
      - ../npm:/home/app/npm
    working_dir: "/home/app/ws"
    command: ./run.sh
    networks:
      - outside
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 256M
        reservations:
          cpus: '0.25'
          memory: 64M
networks:
  outside:
    external:
      name: "host"
